<?xml version='1.0' encoding='UTF-8'?>
<flow-definition plugin="workflow-job">
  <description>Manually builds and deploys the latest IFileManager main commit using the versioned Jenkinsfile from Git.</description>
  <keepDependencies>false</keepDependencies>
  <properties>
    <org.jenkinsci.plugins.workflow.job.properties.DisableConcurrentBuildsJobProperty>
      <abortPrevious>false</abortPrevious>
    </org.jenkinsci.plugins.workflow.job.properties.DisableConcurrentBuildsJobProperty>
    <jenkins.model.BuildDiscarderProperty>
      <strategy class="hudson.tasks.LogRotator">
        <daysToKeep>14</daysToKeep>
        <numToKeep>30</numToKeep>
        <artifactDaysToKeep>-1</artifactDaysToKeep>
        <artifactNumToKeep>-1</artifactNumToKeep>
        <removeLastBuild>false</removeLastBuild>
      </strategy>
    </jenkins.model.BuildDiscarderProperty>
  </properties>
  <definition class="org.jenkinsci.plugins.workflow.cps.CpsFlowDefinition" plugin="workflow-cps">
    <script><![CDATA[
node {
  timestamps {
    stage('Checkout') {
      deleteDir()
      sh '''#!/bin/sh
        set -eu
        git clone --depth 1 --single-branch --branch main https://github.com/kyoungrae/IfileManager.git source
        test -f source/Jenkinsfile
        test -f source/docker-compose.yml
        git -C source rev-parse --verify HEAD
      '''
    }
    dir('source') {
      def runPipeline = load 'Jenkinsfile'
      runPipeline()
    }
  }
}
]]></script>
    <sandbox>true</sandbox>
  </definition>
  <triggers/>
  <disabled>false</disabled>
</flow-definition>
